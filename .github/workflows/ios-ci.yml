name: iOS CI

on:
  pull_request:
  push:
    branches:
      - main
      - "agent/**"
  workflow_dispatch:

concurrency:
  group: ios-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    runs-on: macos-15
    timeout-minutes: 55

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Apply Psychonaut Journal identity
        run: bash scripts/rebrand_project.sh

      - name: Show toolchain and project
        run: |
          xcodebuild -version
          swift --version
          xcodebuild -list -project "PsychonautWiki Journal.xcodeproj"
          grep -nE "DEVELOPMENT_TEAM|PRODUCT_BUNDLE_IDENTIFIER|MY_BUNDLE_DISPLAY_NAME" \
            "PsychonautWiki Journal.xcodeproj/project.pbxproj"

      - name: Resolve Swift packages
        run: |
          xcodebuild \
            -resolvePackageDependencies \
            -project "PsychonautWiki Journal.xcodeproj" \
            -scheme "PsychonautWiki Journal"

      - name: Select simulator
        id: simulator
        shell: bash
        run: |
          DEVICE_ID=$(python3 - <<'PY'
          import json, subprocess
          data = json.loads(subprocess.check_output(["xcrun", "simctl", "list", "devices", "available", "-j"]))
          preferred = []
          fallback = []
          for runtime, devices in data.get("devices", {}).items():
              if "iOS" not in runtime:
                  continue
              for device in devices:
                  if not device.get("isAvailable"):
                      continue
                  fallback.append(device)
                  if device.get("name", "").startswith("iPhone"):
                      preferred.append(device)
          chosen = (preferred or fallback)[0]
          print(chosen["udid"])
          PY
          )
          echo "id=$DEVICE_ID" >> "$GITHUB_OUTPUT"
          echo "Using simulator $DEVICE_ID"

      - name: Build app for simulator
        run: |
          set -o pipefail
          xcodebuild \
            -project "PsychonautWiki Journal.xcodeproj" \
            -scheme "PsychonautWiki Journal" \
            -configuration Debug \
            -destination "platform=iOS Simulator,id=${{ steps.simulator.outputs.id }}" \
            -derivedDataPath DerivedData \
            CODE_SIGNING_ALLOWED=NO \
            build | tee build.log

      - name: Run tests
        run: |
          set -o pipefail
          xcodebuild \
            -project "PsychonautWiki Journal.xcodeproj" \
            -scheme "JournalTests" \
            -configuration Debug \
            -destination "platform=iOS Simulator,id=${{ steps.simulator.outputs.id }}" \
            -derivedDataPath DerivedData \
            CODE_SIGNING_ALLOWED=NO \
            test | tee tests.log

      - name: Package simulator build
        if: success()
        run: |
          APP_PATH=$(find DerivedData/Build/Products/Debug-iphonesimulator -maxdepth 1 -name "*.app" -type d | head -n 1)
          test -n "$APP_PATH"
          ditto -c -k --sequesterRsrc --keepParent "$APP_PATH" PsychonautJournal-Simulator.zip

      - name: Build unsigned device app
        if: success()
        run: |
          set -o pipefail
          xcodebuild \
            -project "PsychonautWiki Journal.xcodeproj" \
            -scheme "PsychonautWiki Journal" \
            -configuration Release \
            -destination "generic/platform=iOS" \
            -derivedDataPath DeviceDerivedData \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            build | tee device-build.log

      - name: Package unsigned IPA
        if: success()
        shell: bash
        run: |
          APP_PATH=$(find DeviceDerivedData/Build/Products/Release-iphoneos -maxdepth 1 -name "*.app" -type d | head -n 1)
          test -n "$APP_PATH"
          rm -rf Payload
          mkdir Payload
          ditto "$APP_PATH" "Payload/$(basename "$APP_PATH")"
          /usr/bin/zip -qry PsychonautJournal-Unsigned.ipa Payload
          shasum -a 256 PsychonautJournal-Unsigned.ipa > PsychonautJournal-Unsigned.ipa.sha256

      - name: Upload simulator app
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: psychonaut-journal-simulator-${{ github.run_number }}
          path: PsychonautJournal-Simulator.zip
          if-no-files-found: error

      - name: Upload unsigned device package
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: psychonaut-journal-unsigned-ipa-${{ github.run_number }}
          path: |
            PsychonautJournal-Unsigned.ipa
            PsychonautJournal-Unsigned.ipa.sha256
          if-no-files-found: error

      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ios-ci-logs-${{ github.run_number }}
          path: |
            build.log
            tests.log
            device-build.log
          if-no-files-found: ignore

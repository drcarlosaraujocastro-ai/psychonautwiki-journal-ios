function renderSafer(){topbar('Safer Use',{large:'Safer Use'});$('#screen').innerHTML=`<div class="section-caption">Research</div><div class="card safer-text">Research duration, subjective effects, adverse effects and combinations in advance. Cross-reference multiple sources and do not infer safety from one database.</div><div class="section-caption">Testing</div><div class="card"><div class="safer-text">Drug-checking services and reagent testing reduce uncertainty about identity and adulterants. They do not prove purity or safety.</div><a class="safer-link" href="https://psychonautwiki.org/wiki/Reagent_testing" target="_blank" rel="noopener">Reagent testing <span>↗</span></a></div><div class="section-caption">Dosage</div><div class="card"><div class="safer-text">Know the intended dose, use an appropriate measuring method, account for delayed onset and avoid rapid escalation. Consumer milligram scales are unreliable for very small quantities.</div><button class="linkrow" data-action="volumetric-calc">Volumetric dosing calculator</button></div><div class="section-caption">Combinations</div><div class="card"><div class="safer-text">Combining CNS depressants is a major overdose risk. Use the Journal interaction screen as a flag, not as a guarantee that a combination is safe.</div><a class="safer-link" href="https://combo.tripsit.me" target="_blank" rel="noopener">Tripsit combination checker <span>↗</span></a></div><div class="section-caption">Set & Setting</div><div class="card safer-text">Physical health, mental state, environment, supervision and responsibilities all change risk. Do not drive or assume responsibility for another person's safety while intoxicated.</div><div class="section-caption">Emergency basics</div><div class="card safer-text">An unconscious person who is breathing should be placed in the recovery position while emergency help is obtained. Severe respiratory depression, seizures, hyperthermia, chest pain, marked agitation or reduced consciousness require urgent medical assessment.</div>`}
function showVolumetricCalculator(){modal(`${modalHeader('Volumetric Dosing')}<div style="padding:0 14px 30px"><div class="form-section"><div class="form-title">Solution</div><div class="card"><div class="fieldrow"><label>Solute</label><input id="vol-solute" type="number" step="any" inputmode="decimal" placeholder="100"><span>mg</span></div><div class="fieldrow"><label>Liquid</label><input id="vol-liquid" type="number" step="any" inputmode="decimal" placeholder="10"><span>mL</span></div><div class="fieldrow"><label>Target</label><input id="vol-target" type="number" step="any" inputmode="decimal" placeholder="10"><span>mg</span></div></div></div><div class="section-title">Result</div><div class="card"><div class="calc-result" id="vol-result">— mL</div><div class="form-hint">Volume = target dose ÷ concentration. This calculator does not verify solubility, identity, purity, stability or suitability of the solvent.</div></div></div>`);const calc=()=>{const s=Number($('#vol-solute').value),l=Number($('#vol-liquid').value),t=Number($('#vol-target').value);$('#vol-result').textContent=s>0&&l>0&&t>=0?`${Number((t/(s/l)).toFixed(4))} mL`:'— mL'};['#vol-solute','#vol-liquid','#vol-target'].forEach(id=>$(id).oninput=calc);calc()}

function renderSettings(){topbar('Settings',{large:'Settings'});const standalone=isStandalone();$('#screen').innerHTML=`${!standalone&&!state.settings.installTipDismissed?`<div class="card install-banner"><strong>Install as an iPhone app</strong>${isIOS()?'<ol class="install-steps"><li>Open this page in Safari.</li><li>Tap Share.</li><li>Tap <b>Add to Home Screen</b>.</li><li>Open Journal from the new Home Screen icon.</li></ol>':'Open this URL in Safari on your iPhone, then Share → Add to Home Screen.'}<button class="linkrow" data-action="dismiss-install" style="margin-top:8px">Got it</button></div>`:''}<div class="section-caption">Privacy & storage</div><div class="card"><div class="kv"><span>Running mode</span><b>${standalone?'Home Screen app':'Browser'}</b></div><div class="kv"><span>Journal storage</span><b>On this device</b></div><button class="linkrow" data-action="persist-storage">Request persistent storage</button></div><div class="section-caption">UI</div><div class="card"><div class="fieldrow"><label>Theme</label><select id="theme-select"><option value="system">System</option><option value="light">Light</option><option value="dark">Dark</option></select></div>${settingToggle('Show dosage dots','showDosageDots')}${settingToggle('Draw redoses individually','drawRedosesIndividually')}${settingToggle('Independent substance heights','independentHeights')}</div><div class="section-caption">Journal Data</div><div class="card"><button class="linkrow" data-action="export">Export Data</button><button class="linkrow" data-action="import">Import Data</button><button class="linkrow" data-action="new-custom">Create Custom Substance</button></div><div class="section-footer">Export creates a Journal-compatible JSON file and, on iPhone, opens the Share sheet when supported. Import replaces the current journal after confirmation.</div><div class="section-caption">About</div><div class="card"><div class="kv"><span>Catalog</span><b>${catalog.length} substances</b></div><div class="kv"><span>Offline</span><b>Service Worker</b></div><div class="kv"><span>Web build</span><b>2.1 iPhone PWA</b></div></div><div class="section-caption">Danger Zone</div><div class="card"><button class="destructive" data-action="reset">Delete Everything</button></div><input id="import-file" class="hidden" type="file" accept="application/json,.json">`;const ts=$('#theme-select');ts.value=state.settings.theme||'system';ts.onchange=async e=>{state.settings.theme=e.target.value;applyTheme();await saveState()}}
function settingToggle(label,key){return`<div class="fieldrow"><label style="flex:1">${esc(label)}</label><button class="switch ${state.settings[key]?'on':''}" data-toggle-setting="${key}" aria-label="${esc(label)}"></button></div>`}

function modal(html,sheet=false){$('#modal-root').innerHTML=sheet?`<div class="modal-sheet"><div class="sheet-content">${html}</div></div>`:`<div class="modal">${html}</div>`}
function closeModal(){window.__ingDraft=null;window.__customDraft=null;$('#modal-root').innerHTML=''}
function modalHeader(title,right='',cancel='Cancel'){return`<div class="modal-head"><div class="navrow"><div class="navleft"><button class="navbtn" data-close-modal>${cancel}</button></div><div class="navtitle">${esc(title)}</div><div class="navright">${right}</div></div></div>`}

function startIngestionWizard(prefill='',experienceId=''){window.__ingDraft={step:1,substanceName:prefill||'',route:'',dose:null,units:'mg',estimated:false,time:Date.now(),stomach:'',experienceId:experienceId||'',notes:''};if(prefill){const s=findSubstance(prefill);window.__ingDraft.route=String(s?.roas?.[0]?.name||'oral').toUpperCase();window.__ingDraft.units=routeInfo(s,window.__ingDraft.route)?.dose?.units||s?.units||'mg';window.__ingDraft.step=2}renderIngestionWizard()}
function wizardHeader(title,step,right=''){return`${modalHeader(title,right)}<div class="wizard-progress"><div style="width:${step/4*100}%"></div></div>`}
function renderIngestionWizard(){const d=window.__ingDraft;if(!d)return;const s=findSubstance(d.substanceName);if(d.step===1){const list=allSubstances().filter(x=>!d.search||[x.name,...(x.commonNames||[])].some(n=>String(n).toLowerCase().includes(d.search.toLowerCase()))).slice(0,120);modal(`${wizardHeader('Choose Substance',1)}<div style="padding:10px 14px 30px"><div class="searchbox"><input id="wiz-search" placeholder="Search substances" value="${esc(d.search||'')}" autocomplete="off"></div><div class="card">${list.map(x=>`<button class="list-row choice-row" data-pick-substance="${esc(x.name)}"><span class="dot" style="background:${x.isCustom?colorFor(x.name):'#8e8e93'}"></span><div class="row-main"><div class="row-title">${esc(x.name)}</div><div class="row-sub">${esc((x.categories||[]).slice(0,3).join(' · '))}</div></div><span class="chev">›</span></button>`).join('')}</div></div>`);$('#wiz-search').oninput=e=>{d.search=e.target.value;renderIngestionWizard();const i=$('#wiz-search');i?.focus();i?.setSelectionRange(d.search.length,d.search.length)};return}
 if(d.step===2){const roas=s?.roas?.length?s.roas:[{name:'oral'}];modal(`${wizardHeader('Route',2)}<div style="padding:10px 14px 30px"><div class="section-title">${esc(d.substanceName)}</div><div class="card">${roas.map(r=>`<button class="list-row choice-row ${String(d.route).toLowerCase()===String(r.name).toLowerCase()?'selected':''}" data-pick-route="${esc(String(r.name).toUpperCase())}"><div class="row-main"><div class="row-title">${esc(String(r.name).replaceAll('_',' '))}</div></div><span class="chev">›</span></button>`).join('')}${!roas.some(r=>String(r.name).toUpperCase()==='OTHER')?'<button class="list-row choice-row" data-pick-route="OTHER"><div class="row-main"><div class="row-title">Other</div></div><span class="chev">›</span></button>':''}</div></div>`);return}
 if(d.step===3){const roa=routeInfo(s,d.route),dose=roa?.dose||{};const guide=[['Threshold',dose.threshold??dose.lightMin],['Common',dose.commonMin],['Strong',dose.strongMin],['Heavy',dose.heavyMin]].filter(x=>x[1]!=null);modal(`${wizardHeader('Dose',3,`<button class="navbtn" data-wiz-next>Next</button>`)}<div style="padding:10px 14px 30px"><div class="section-title">${esc(d.substanceName)} · ${esc(d.route)}</div><div class="card"><div class="fieldrow"><label>Dose</label><input id="wiz-dose" type="number" step="any" inputmode="decimal" placeholder="0" value="${d.dose??''}"><input id="wiz-units" style="max-width:68px" value="${esc(d.units||dose.units||'mg')}"></div><div class="fieldrow"><label style="flex:1">Dose is an estimate</label><button class="switch ${d.estimated?'on':''}" data-action="wiz-estimate"></button></div></div>${guide.length?`<div class="section-caption">Reference bins</div><div class="card">${guide.map(([k,v])=>`<button class="dose-choice choice-row" data-dose-value="${v}"><span>${k}</span><b>${v} ${esc(dose.units||d.units||'')}</b></button>`).join('')}</div>`:''}</div>`);return}
 const exps=[...state.experiences].sort((a,b)=>(b.sortDate||0)-(a.sortDate||0));const warnings=interactionWarnings(d.substanceName,d.experienceId);modal(`${wizardHeader('Finish',4,`<button class="navbtn" data-save-wizard>Done</button>`)}<div style="padding:10px 14px 30px"><div class="section-title">${esc(d.substanceName)}</div>${warnings.length?`<div class="section-caption">Interaction flags</div><div class="stack">${warnings.map(w=>`<div class="card alert-card ${w.severity}"><strong>${w.severity[0].toUpperCase()+w.severity.slice(1)} · ${esc(w.with)}</strong>${esc(w.reason)}</div>`).join('')}</div>`:''}<div class="form-section"><div class="form-title">Time</div><div class="card"><div class="fieldrow"><label>Date & time</label><input id="wiz-time" type="datetime-local" value="${localDateTimeValue(d.time)}"></div><div class="fieldrow"><label>Stomach</label><select id="wiz-stomach">${STOMACH.map(v=>`<option value="${v}" ${d.stomach===v?'selected':''}>${v||'Not set'}</option>`).join('')}</select></div></div></div><div class="form-section"><div class="form-title">Experience</div><div class="card"><div class="fieldrow"><label>Add to</label><select id="wiz-exp"><option value="">New experience</option>${exps.map(e=>`<option value="${e.id}" ${d.experienceId===e.id?'selected':''}>${esc(e.title||fmtDate(e.sortDate))}</option>`).join('')}</select></div></div></div><div class="form-section"><div class="form-title">Notes</div><div class="card"><div class="fieldrow"><textarea id="wiz-notes" placeholder="Notes">${esc(d.notes||'')}</textarea></div></div></div>${warnings.some(w=>w.severity==='dangerous')?'<div class="section-footer danger">A dangerous interaction flag is not a diagnosis, but it is a strong reason not to treat the combination as routine. The database may be incomplete.</div>':''}</div>`)}
function saveWizardFields(){const d=window.__ingDraft;if(!d)return;if($('#wiz-dose')){d.dose=$('#wiz-dose').value===''?null:Number($('#wiz-dose').value);d.units=$('#wiz-units').value||'mg'}if($('#wiz-time'))d.time=new Date($('#wiz-time').value).getTime();if($('#wiz-stomach'))d.stomach=$('#wiz-stomach').value;if($('#wiz-exp'))d.experienceId=$('#wiz-exp').value;if($('#wiz-notes'))d.notes=$('#wiz-notes').value}
function tokenMatchesSubstance(token,sub){const t=String(token).toLowerCase();if(String(sub?.name).toLowerCase()===t)return true;return(sub?.categories||[]).some(c=>String(c).toLowerCase()===t)}
function interactionBetween(a,b){if(!a||!b)return null;const order=['dangerous','unsafe','uncertain'];for(const sev of order){for(const token of a.interactions?.[sev]||[]){if(tokenMatchesSubstance(token,b))return{severity:sev,reason:`${a.name} lists ${token} as ${sev}.`}}for(const token of b.interactions?.[sev]||[]){if(tokenMatchesSubstance(token,a))return{severity:sev,reason:`${b.name} lists ${token} as ${sev}.`}}}return null}
function interactionWarnings(name,experienceId){const exp=getExperience(experienceId);if(!exp)return[];const a=findSubstance(name),seen=new Set(),out=[];for(const i of exp.ingestions||[]){if(seen.has(i.substanceName)||String(i.substanceName).toLowerCase()===String(name).toLowerCase())continue;seen.add(i.substanceName);const b=findSubstance(i.substanceName),hit=interactionBetween(a,b);if(hit)out.push({...hit,with:i.substanceName})}return out}
async function finishIngestionWizard(){saveWizardFields();const d=window.__ingDraft;if(!d?.substanceName||!Number.isFinite(d.time)){toast('Missing substance or invalid time.');return}const ing={id:uuid(),substanceName:d.substanceName,time:d.time,endTime:null,creationDate:nowMs(),administrationRoute:d.route||'ORAL',dose:d.dose,estimatedDoseStandardDeviation:null,isDoseAnEstimate:!!d.estimated,units:d.units||'mg',notes:d.notes||'',stomachFullness:d.stomach||null,consumerName:null,customUnitId:null};let exp=getExperience(d.experienceId);if(!exp){exp={id:uuid(),title:titleForDate(d.time),text:'',creationDate:nowMs(),sortDate:d.time,isFavorite:false,ingestions:[],ratings:[],timedNotes:[],location:null};state.experiences.push(exp)}exp.ingestions.push(ing);exp.sortDate=Math.min(...exp.ingestions.map(i=>i.time));if(!state.companions[d.substanceName])state.companions[d.substanceName]=findSubstance(d.substanceName)?.color||Object.keys(COLORS)[Object.keys(state.companions).length%Object.keys(COLORS).length];await saveState();closeModal();selectedExperienceId=exp.id;toast('Ingestion saved');currentTab='journal';tabs();navigate('experience',exp.id)}
